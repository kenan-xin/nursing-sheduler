---
title: "Review — PH default group + group descriptions (code + specs)"
kind: review
---

# Review — PH default group + group descriptions

**Scope: the new **`PH (Public Holidays) default date group and the expanded`
group descriptions (WORKDAY / NON-WORKDAY / PH, both seeded and imported), across
both code checkouts and both spec copies. Reviewed cold by a fresh agent (the
change was authored in the originating session). No files modified during review.

**Verdict: PASS. One LOW citation drift found and fixed; no correctness,**
wiring, parity, or contract issues.

## Finding (fixed)

- **LOW — stale line citations in FR-DC-34 (**`02-dates-and-calendar/index.md:137).`
Cited `singaporeHolidays.ts:231-269 / singaporeHolidays.test.ts:260-277`
(the pre-PH 2-group version). The function now spans `235-283. Prose was`
correct ("returns exactly three groups"); only the anchors drifted.
**Fixed → **`singaporeHolidays.ts:235-283 / singaporeHolidays.test.ts:261-278.`

## Verified (all correct)

- **PH membership: **`buildSingaporeHolidayGroups pushes to PH iff the item's`
date is in the public-holiday set; to NON-WORKDAY iff PH OR UTC weekend; else
WORKDAY. Holiday-on-weekend → both PH and NON-WORKDAY; plain weekend →
NON-WORKDAY only; observed substitutes → included in PH. `PH ⊆ NON-WORKDAY.`
No inversion/off-by-one. Backed by tests (`singaporeHolidays.test.ts:280-381).`
- **Three-group return + wiring: returns WORKDAY/NON-WORKDAY/PH;**
`updateDateRange → replaceDateGroups (useSchedulingData.ts:192) appends PH,`
never drops it. `PH is not in the auto-generated catalog and is not a reserved`
keyword, so it is neither stripped nor blocked.
- **Default seed: three empty groups in order, no **`isAutoGenerated, exact`
descriptions.
- **Descriptions exact: all six strings (3 seed + 3 imported) match; tests**
assert the same strings.
- **Two-checkout parity: changed files byte-identical (canonical vs worktree).**
- **Spec + spec-copy parity: specs 01/02/07 + decision-log 04 reflect three**
groups and the new descriptions; no residual "two groups" language; source and
synced spec copies identical.
- **ID value consistency: no **`NONWORKDAY (no-hyphen) used as an id value; the`
identifier maps to value `'NON-WORKDAY' consistently.`

## Intentional scope boundaries (confirmed, not defects)

- No PH OFF-count column in `schedulingExportConfig.`
- `getSingaporeDayType still returns only 'WORKDAY' | 'NON-WORKDAY' — PH is a`
membership group, not a calendar day-type.

## Verification basis

- Frontend unit suite: 623 passed. Cold agent read-only; findings confirmed
against cited lines.
